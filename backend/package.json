{
  "name": "popcoon-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Popcoon price history + alerting backend on Cloudflare Workers",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260706.1",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0",
    "vitest": "^2.1.0",
    "@cloudflare/vitest-pool-workers": "^0.5.0"
  },
  "engines": {
    "node": ">=20"
  }
}
